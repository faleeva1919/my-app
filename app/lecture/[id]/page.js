import Link from "next/link";

export default async function Lecture({ params }) {
  const { id } = await params;

  return (
    <div className="container">
      {id === "1" && <Lecture1 />}
      {id === "2" && <Lecture2 />}
      {id === "3" && <Lecture3 />}
      {id === "4" && <Lecture4 />}
      {id === "5" && <Lecture5 />}
      {id === "6" && <Lecture6 />}
      {id === "7" && <Lecture7 />}
      {id === "8" && <Lecture8 />}

      {/* навигация */}
      <div className="card">
        <Link href={`/practice/${id}`}>⬅ К практике</Link>
      </div>

      <div className="card">
        <Link href="/">На главную</Link>
      </div>
    </div>
  );
}

/* ================= ЛЕКЦИЯ 1 ================= */

function Lecture1() {
  return (
    <>
      <h1>📘 Лекция 1. Введение в AppSheet</h1>

      <div className="card">
        <h2>1. Что такое low-code платформы и место AppSheet среди них</h2>

        <p>
          Low-code — это когда приложение собирается из готовых блоков, а не пишется вручную тысячами строк кода. 
          Вместо команды из бэкендщиков, фронтендщиков и тестировщиков вы работаете сами или маленькой группой. 
          Время разработки падает с месяцев до дней и даже часов.
        </p>

        <p>
          AppSheet — это low-code платформа, которую Google купил в 2020 году. 
          Она плотно встроена в экосистему Google Workspace, но главное не это. 
          Главное — её логика: вы сначала подключаете данные, а уже из них рождается приложение, а не наоборот.
        </p>

        <p>
          В чём разница с другими low-code системами? Обычно вы сначала создаёте базу данных, 
          потом на неё вешаете интерфейс. В AppSheet вы работаете с тем, что уже есть. 
          Чаще всего — это Google Таблица.
        </p>

        <ul>
          <li>AppSheet не хранит данные — он только прослойка</li>
          <li>Данные остаются у вас</li>
          <li>Поддержка Google Sheets, Excel, SQL, Salesforce</li>
        </ul>

        <p>
          Из важных плюсов:
        </p>

        <ul>
          <li>Мобильное приложение создаётся автоматически</li>
          <li>Работает офлайн</li>
          <li>Поддержка QR-кодов, фото, геометок</li>
          <li>Распознавание текста (OCR)</li>
          <li>Быстрая синхронизация с Google Таблицами</li>
        </ul>

        <p>
          ❗ Минус: при большом количестве данных и пользователей приложение может тормозить.
        </p>
      </div>

      <div className="card">
        <h2>2. Архитектура AppSheet</h2>

        <p>
          Устройство AppSheet жёсткое и логичное. Всё строится на трёх уровнях.
        </p>

        <h3>📊 Уровень 1 — источник данных</h3>
        <ul>
          <li>Один лист = одна сущность</li>
          <li>Столбец = поле</li>
          <li>Строка = запись</li>
        </ul>

        <p>
          Структура таблицы полностью определяет приложение.
        </p>

        <h3>⚙️ Уровень 2 — логика и безопасность</h3>
        <ul>
          <li>Кто что видит</li>
          <li>Проверка данных</li>
          <li>Автоматические действия</li>
          <li>Боты (автоматизация процессов)</li>
        </ul>

        <h3>🖥 Уровень 3 — интерфейс</h3>
        <ul>
          <li>Таблица</li>
          <li>Форма</li>
          <li>Календарь</li>
          <li>Kanban</li>
          <li>Карта</li>
        </ul>

        <p>
          Интерфейс автоматически адаптируется под устройство и поддерживает офлайн-режим.
        </p>
      </div>

      <div className="card">
        <h2>3. Интерфейс AppSheet</h2>

        <p>Основные разделы редактора:</p>

        <ul>
          <li><b>Data → Tables</b> — таблицы и поля</li>
          <li><b>Data → Slices</b> — срезы данных</li>
          <li><b>UX → Views</b> — экраны приложения</li>
          <li><b>Automation → Bots</b> — автоматизация</li>
        </ul>

        <p>
          ❗ Важно: в таблице должен быть уникальный ключ (ID)
        </p>
      </div>

      <div className="card">
        <h2>4. Создание приложения</h2>

        <h3>Подготовка таблицы:</h3>
        <ul>
          <li>Создать Google Таблицу</li>
          <li>Добавить столбец ID</li>
          <li>Заполнить несколько строк</li>
        </ul>

        <h3>Создание:</h3>
        <ol>
          <li>Нажать Create → App</li>
          <li>Выбрать Google Sheets</li>
          <li>Указать таблицу</li>
          <li>Нажать Create App</li>
        </ol>

        <p>
          AppSheet автоматически создаёт:
        </p>

        <ul>
          <li>Список</li>
          <li>Форму</li>
          <li>Карточку</li>
          <li>Навигацию</li>
        </ul>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>
        <ol>
          <li>Что такое low-code разработка?</li>
          <li>Как работает AppSheet с данными?</li>
          <li>Какие уровни архитектуры?</li>
          <li>Зачем нужен ID?</li>
          <li>Какие интерфейсы создаются автоматически?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 2 ================= */

function Lecture2() {
  return (
    <>
      <h1>📘 Лекция 2. Источники данных: Google Таблицы</h1>

      <div className="card">
        <p>
          AppSheet не хранит данные сам. Он только подключается к тому, что уже есть.
          Поэтому от того, как вы оформите таблицу, зависит, будет ли приложение работать вообще.
          Плохая таблица = кривое приложение. Хорошая таблица = AppSheet сам всё поймёт и сделает.
        </p>
      </div>

      <div className="card">
        <h2>1. Главный принцип работы</h2>

        <p>AppSheet работает как прослойка между пользователем и таблицей.</p>

        <ul>
          <li>Один лист = одна сущность</li>
          <li>Один столбец = одно поле</li>
          <li>Одна строка = одна запись</li>
        </ul>

        <p>
          Структура таблицы полностью определяет структуру приложения.
        </p>
      </div>

      <div className="card">
        <h2>2. Правила оформления таблицы</h2>

        <h3>Правило 1. Заголовки</h3>
        <ul>
          <li>Первая строка — заголовки</li>
          <li>Уникальные названия</li>
          <li>Лучше без пробелов</li>
        </ul>

        <h3>Правило 2. Строки</h3>
        <ul>
          <li>Каждая строка — запись</li>
          <li>Нет пустых строк</li>
          <li>Нет объединённых ячеек</li>
        </ul>

        <h3>Правило 3. Ключ (Key)</h3>
        <ul>
          <li>Уникальный столбец</li>
          <li>Например: ID, Email</li>
        </ul>

        <h3>Правило 4. Без формул</h3>
        <p>
          Формулы нельзя редактировать через приложение — используйте AppSheet.
        </p>
      </div>

      <div className="card">
        <h2>3. Чек-лист</h2>
        <ol>
          <li>На первом листе в первой строке — заголовки столбцов</li>
          <li>Есть столбец с уникальными ID (ключ)</li>
          <li>Заполнены 3–5 строк тестовыми данными</li>
          <li>Нет пустых строк и объединённых ячеек</li>
          <li>Нет формул в ячейках (если данные будут редактироваться)</li>
        </ol>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>
        <ol>
          <li>Какие источники данных используются?</li>
          <li>Какие правила таблицы?</li>
          <li>Что такое Key?</li>
          <li>Почему нельзя формулы?</li>
          <li>Что будет при ошибках в таблице?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 3 ================= */

function Lecture3() {
  return (
    <>
      <h1>📘 Лекция 3. Типы данных и связи между таблицами</h1>

      <div className="card">
        <h2>1. Основные типы данных в AppSheet</h2>

        <p>
          AppSheet автоматически определяет тип данных по первым строкам таблицы. 
          Если платформа ошиблась (например, столбец с датами определился как текст), 
          разработчик может изменить тип вручную в редакторе.
        </p>

        <p>
          Text — строковое значение. Используется для имён, адресов, описаний, номеров документов. 
          Максимальная длина ограничена источником данных.
        </p>

        <p>
          Number — числовое значение. Используется для количества, цены, ID (если это число). 
          Поддерживает десятичные дроби.
        </p>

        <p>
          True/False — логическое значение (да/нет). В интерфейсе отображается как переключатель (чекбокс).
        </p>

        <p>
          Date — только дата без времени. Используется для даты заказа, даты рождения, дедлайна.
        </p>

        <p>
          DateTime — дата и время вместе. Используется для времени создания записи, начала встречи.
        </p>

        <p>
          Email — адрес электронной почты. При нажатии на такое поле в приложении открывается почтовый клиент.
        </p>

        <p>
          Phone — номер телефона. На мобильных устройствах при нажатии открывается набор номера.
        </p>

        <p>
          Image — изображение. В таблице хранится ссылка на файл, а в приложении показывается картинка. 
          Пользователь может загрузить фото с камеры или из галереи.
        </p>

        <p>
          Enum — выбор одного значения из списка. Например, статус заказа: «Новый», «В обработке», «Доставлен». 
          В интерфейсе — выпадающий список.
        </p>

        <p>
          EnumList — выбор нескольких значений из списка. Например, теги для задачи: «Срочно», «Важно», «Бюджет». 
          Пользователь может выбрать несколько вариантов.
        </p>

        <p>
          Ref — ссылка на запись в другой таблице. Используется для связей между таблицами. 
          Например, в заказе указать, к какому клиенту он относится.
        </p>
      </div>

      <div className="card">
        <h2>2. Выбор из списка (Enum) и работа с изображениями</h2>

        <p>
          Enum (выпадающий список) ограничивает возможные значения предопределённым набором. 
          Это удобно для статусов, категорий, типов, потому что пользователь не может ввести что-то левое.
        </p>

        <p>Способы создания Enum:</p>

        <ul>
          <li>
            Фиксированный список: значения задаются вручную в редакторе 
            (например: «Новый», «В обработке», «Завершён»)
          </li>
          <li>
            Из столбца другой таблицы: источником служит столбец справочника 
            (например, список городов из таблицы «Города»)
          </li>
        </ul>

        <p>
          Пример настройки Enum для статуса заказа: «Новый», «В обработке», 
          «Отправлен», «Доставлен», «Отменён». Пользователь может выбрать только один из этих вариантов.
        </p>

        <p>
          Изображения. AppSheet не хранит сами картинки, только ссылки на них. 
          Это экономит место и ускоряет работу.
        </p>

        <p>Способы работы с изображениями:</p>

        <ul>
          <li>Прямые ссылки на файлы (https://...)</li>
          <li>Хранение в Google Drive</li>
          <li>Загрузка через интерфейс приложения</li>
        </ul>

        <p>
          Требования: столбец должен иметь тип Image, а пользователи должны иметь доступ к файлам.
        </p>
      </div>

      <div className="card">
        <h2>3. Связи между таблицами: ключи и ссылки</h2>

        <p>
          В реальных приложениях данные редко хранятся в одной таблице. 
          Связи позволяют избежать дублирования и организовать структуру.
        </p>

        <p>
          Key (Ключ) — уникальный идентификатор записи в таблице. 
          Каждая таблица должна иметь ключ.
        </p>

        <p>
          Ref (Ссылка) — тип данных, который указывает на запись в другой таблице.
        </p>

        <p>
          Связь «один ко многим» — самый распространённый тип связи. 
          Одна запись может иметь много связанных записей.
        </p>

        <p>Пример: один клиент может иметь много заказов.</p>

        <p>Реализация в AppSheet:</p>

        <ol>
          <li>Создать ключевое поле «КлиентID»</li>
          <li>Создать поле Ref в таблице «Заказы»</li>
          <li>AppSheet автоматически создаст обратную ссылку</li>
        </ol>
      </div>

      <div className="card">
        <h2>4. Обратные ссылки (Reverse Ref)</h2>

        <p>
          Когда вы создаёте Ref-поле, AppSheet автоматически создаёт виртуальный столбец 
          в родительской таблице. Это и есть обратная ссылка.
        </p>

        <p>Что даёт обратная ссылка:</p>

        <ul>
          <li>Показ всех связанных записей</li>
          <li>Агрегации (COUNT, SUM)</li>
          <li>Удобная навигация</li>
        </ul>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Какие типы данных используются для выбора из списка?</li>
          <li>Как AppSheet работает с изображениями?</li>
          <li>Что такое Key и Ref?</li>
          <li>Что такое связь «один ко многим»?</li>
          <li>Что даёт обратная ссылка?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 4 ================= */

function Lecture4() {
  return (
    <>
      <h1>📘 Лекция 4. Виды интерфейса (Views)</h1>

      <div className="card">
        <h2>1. Что такое виды и зачем они нужны</h2>

        <p>
          Виды (Views) — это способ отображения данных для пользователя. 
          Каждый вид определяет, как выглядит экран приложения, какие данные на нём показываются 
          и как пользователь с ними взаимодействует.
        </p>

        <p>
          При создании приложения из Google Таблицы AppSheet автоматически генерирует базовый набор видов:
        </p>

        <ul>
          <li>Табличный вид — список всех записей</li>
          <li>Форма — для добавления новых записей</li>
          <li>Детальная карточка — для просмотра одной записи</li>
        </ul>

        <p>
          Разработчик может добавлять новые виды, удалять существующие и настраивать их параметры.
        </p>

        <p>
          Все виды делятся на категории:
        </p>

        <ul>
          <li>Виды данных — отображают набор записей</li>
          <li>Детальные виды — показывают одну запись</li>
          <li>Виды действий — создание и редактирование</li>
          <li>Составные виды — объединяют несколько экранов (дашборд)</li>
        </ul>
      </div>

      <div className="card">
        <h2>2. Основные типы видов</h2>

        <p>
          Таблица (Table) — классическое табличное представление. Строки соответствуют записям, 
          столбцы — полям. При нажатии открывается детальная карточка. Подходит для работы 
          с большим количеством данных.
        </p>

        <p>
          Форма (Form) — используется для создания и редактирования записей. Поля расположены вертикально, 
          пользователь заполняет их и отправляет форму.
        </p>

        <p>
          Календарь (Calendar) — отображает записи с датами в виде календаря. 
          Удобен для расписаний и задач.
        </p>

        <p>
          Галерея (Gallery) — показывает записи в виде карточек с изображениями. 
          Подходит для товаров, портфолио и фотоотчётов.
        </p>

        <p>
          Карточки (Kanban) — отображают записи в виде колонок по статусу. 
          Записи можно перетаскивать между колонками.
        </p>

        <p>
          Карта (Map) — отображает записи на карте по координатам или адресам.
        </p>

        <p>
          Дашборд (Dashboard) — объединяет несколько видов на одном экране и используется 
          как главный экран приложения.
        </p>
      </div>

      <div className="card">
        <h2>3. Иерархия видов в приложении</h2>

        <p>
          Виды в AppSheet организованы в иерархическую структуру.
        </p>

        <ul>
          <li>
            Корневые виды — отображаются в навигационном меню и являются основными экранами приложения.
          </li>
          <li>
            Дочерние виды — открываются из других видов (например, форма или детальная карточка).
          </li>
        </ul>

        <p>
          Иерархия настраивается в разделе UX → Navigation.
        </p>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Какие виды создаются автоматически?</li>
          <li>Когда использовать Таблицу, а когда Галерею?</li>
          <li>Как работает Kanban?</li>
          <li>Чем отличается корневой вид от дочернего?</li>
          <li>Что такое Dashboard?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 5 ================= */

function Lecture5() {
  return (
    <>
      <h1>📘 Лекция 5. Настройка форм и отображение данных</h1>

      <div className="card">
        <h2>1. Настройка формы ввода</h2>

        <p>
          Форма в AppSheet строится на основе столбцов таблицы. Каждому столбцу соответствует поле ввода, 
          тип которого определяется типом данных столбца.
        </p>

        <p>
          Группировка полей (Sections) — для улучшения восприятия длинных форм можно группировать поля 
          по логическим блокам. Например, для формы заказа можно создать группы: «Информация о заказе», 
          «Информация о клиенте», «Финансовая информация». Поля с одинаковым названием группы объединяются визуально.
        </p>

        <p>
          Обязательные поля (Required) — поля, которые пользователь должен заполнить перед сохранением. 
          Если обязательное поле не заполнено, запись не сохранится. Рекомендуется делать обязательными ключевые поля.
        </p>

        <p>
          Значения по умолчанию (Initial Value) — автоматически подставляемые значения при создании новой записи. 
          Можно использовать формулы: TODAY(), USEREMAIL(). 
          Примеры: дата создания = TODAY(), автор = USEREMAIL(), статус = "Новый".
        </p>

        <p>
          Подсказки (Hint Text) — текст внутри поля ввода до его заполнения. 
          Примеры: "example@company.com", "+7 XXX XXX-XX-XX".
        </p>

        <p>
          Валидация данных (Valid If) — условие, которому должно соответствовать значение поля. 
          Примеры: возраст от 18 до 100 — [Возраст] &gt;= 18 AND [Возраст] &lt;= 100, 
          email содержит @ — CONTAINS([Email], "@"), 
          дата окончания не раньше начала — [Дата окончания] &gt;= [Дата начала].
        </p>
      </div>

      <div className="card">
        <h2>2. Фильтрация данных</h2>

        <p>
          Фильтры позволяют отображать только те записи, которые соответствуют определённым условиям. 
          Это один из самых важных инструментов, потому что никто не хочет листать тысячи записей.
        </p>

        <p>
          Настройка фильтра для вида выполняется через параметр Filter.
        </p>

        <p>Примеры фильтров:</p>

        <ul>
          <li>[Статус] &lt;&gt; "Завершен" — только активные заказы</li>
          <li>[Менеджер] = USEREMAIL() — только записи пользователя</li>
          <li>[Дедлайн] &lt; TODAY() AND [Статус] &lt;&gt; "Выполнено" — просроченные задачи</li>
          <li>[Дата] &gt;= TODAY() - 7 — записи за последние 7 дней</li>
        </ul>

        <p>
          Фильтры можно задавать через срезы (Slices). Срез — это виртуальная копия таблицы с фильтром.
        </p>

        <p>
          Преимущества срезов:
        </p>

        <ul>
          <li>Фильтр применяется ко всем видам</li>
          <li>Можно ограничивать доступ</li>
          <li>Упрощает управление данными</li>
        </ul>

        <p>
          Пример: срез «Мои задачи» с фильтром [Ответственный] = USEREMAIL().
        </p>
      </div>

      <div className="card">
        <h2>3. Сортировка и группировка данных</h2>

        <p>
          Сортировка (Sorting) определяет порядок отображения записей.
        </p>

        <p>Примеры сортировки:</p>

        <ul>
          <li>По дате создания — новые сверху</li>
          <li>По фамилии — по алфавиту</li>
          <li>Сначала приоритет, затем дедлайн</li>
        </ul>

        <p>
          Группировка (Grouping) объединяет записи по значению поля.
        </p>

        <p>Пример группировки заказов по статусу:</p>

        <ul>
          <li>Новые → список заказов</li>
          <li>В обработке → список заказов</li>
          <li>Доставлен → список заказов</li>
        </ul>

        <p>
          Группировка помогает быстрее ориентироваться в данных.
        </p>
      </div>

      <div className="card">
        <h2>4. Настройка внешнего вида записей</h2>

        <p>
          Заголовки записей (Record Title) — это текст, который пользователь видит в интерфейсе.
        </p>

        <p>Примеры:</p>

        <ul>
          <li>[Номер заказа] &amp; " - " &amp; [Клиент].[Название]</li>
          <li>[Фамилия] &amp; " " &amp; LEFT([Имя], 1) &amp; "."</li>
          <li>[Название] &amp; " (" &amp; [Приоритет] &amp; ")"</li>
        </ul>

        <p>
          Условное форматирование позволяет выделять записи цветом.
        </p>

        <p>Примеры:</p>

        <ul>
          <li>Просрочено → красный</li>
          <li>Высокий приоритет → жёлтый</li>
          <li>Завершено → серый</li>
        </ul>

        <p>
          Настройка галереи включает Image, Title, Subtitle и Body.
        </p>

        <p>
          Настройка календаря включает Start, End, Color и Title.
        </p>

        <p>
          Детальная карточка (Detail View) показывает все поля записи и может настраиваться 
          по структуре и отображению.
        </p>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Что такое группировка полей?</li>
          <li>Как задать значение по умолчанию?</li>
          <li>Чем отличается фильтр от сортировки?</li>
          <li>Что такое Slice?</li>
          <li>Как сделать цвет для просроченных задач?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 6 ================= */

function Lecture6() {
  return (
    <>
      <h1>📘 Лекция 6. Формулы и виртуальные столбцы</h1>

      <div className="card">
        <h2>1. Основы формул в AppSheet</h2>

        <p>
          Формулы — это главный инструмент для реализации логики в AppSheet. 
          Они используются для вычисляемых полей, валидации данных, фильтрации, 
          условного форматирования и настройки поведения приложения.
        </p>

        <p>Базовые правила синтаксиса:</p>

        <ul>
          <li>Обращение к полям — [Название поля]</li>
          <li>Строки — "Текст"</li>
          <li>Числа — 100, 15.5</li>
          <li>Логические значения — TRUE, FALSE</li>
          <li>Связанные данные — [Клиент].[Email]</li>
          <li>Комментарии — // комментарий</li>
        </ul>

        <p>Операторы:</p>

        <ul>
          <li>Арифметические: +, -, *, /</li>
          <li>Сравнения: =, &lt;&gt;, &lt;, &gt;, &lt;=, &gt;=</li>
          <li>Логические: AND, OR, NOT</li>
          <li>Строки: &amp;</li>
        </ul>

        <p>Основные функции:</p>

        <p><b>Текстовые:</b></p>
        <ul>
          <li>CONCATENATE()</li>
          <li>LEFT(), RIGHT()</li>
          <li>CONTAINS()</li>
        </ul>

        <p><b>Числовые:</b></p>
        <ul>
          <li>SUM(), COUNT()</li>
          <li>ROUND()</li>
        </ul>

        <p><b>Дата и время:</b></p>
        <ul>
          <li>TODAY()</li>
          <li>NOW()</li>
          <li>YEAR(), MONTH(), DAY()</li>
        </ul>

        <p><b>Пользователь:</b></p>
        <ul>
          <li>USEREMAIL()</li>
          <li>USERNAME()</li>
        </ul>

        <p>
          Пустые значения обозначаются как BLANK().
        </p>

        <pre>
{`ISBLANK([Поле])
ISNOTBLANK([Поле])`}
        </pre>
      </div>

      <div className="card">
        <h2>2. Виртуальные столбцы (Virtual Columns)</h2>

        <p>
          Виртуальные столбцы — это поля, которые не хранятся в таблице, 
          а вычисляются по формуле внутри AppSheet.
        </p>

        <p>Как создать:</p>

        <ol>
          <li>Data → Tables</li>
          <li>Выбрать таблицу</li>
          <li>Add new column</li>
          <li>Включить Virtual</li>
        </ol>

        <p>Примеры использования:</p>

        <p><b>Агрегация:</b></p>
        <pre>
{`SUM([Заказы].[Сумма])`}
        </pre>

        <p><b>Объединение полей:</b></p>
        <pre>
{`CONCATENATE([Фамилия], " ", [Имя])`}
        </pre>

        <p><b>Форматирование:</b></p>
        <pre>
{`CONCATENATE("Заказ от ", TEXT([Дата], "dd.MM.yyyy"))`}
        </pre>

        <p><b>Категория по цене:</b></p>
        <pre>
{`IF([Цена] < 1000, "Бюджетный",
IF([Цена] < 5000, "Средний", "Премиум"))`}
        </pre>

        <p><b>Статус задачи:</b></p>
        <pre>
{`IF([Дата завершения] > [Дедлайн], "Просрочена",
IF([Статус] = "Выполнено", "Завершена", "В работе"))`}
        </pre>
      </div>

      <div className="card">
        <h2>3. Условные выражения: IF и SWITCH</h2>

        <p>
          Условные выражения позволяют строить логику: если условие выполняется — одно, иначе — другое.
        </p>

        <p><b>IF:</b></p>

        <pre>
{`IF([Сумма] > 10000, "Крупный заказ", "Обычный заказ")`}
        </pre>

        <pre>
{`IF(ISBLANK([Дата отгрузки]), "Не отгружен", "Отгружен")`}
        </pre>

        <p><b>Вложенный IF:</b></p>

        <pre>
{`IF([Сумма] >= 10000, "VIP",
  IF([Сумма] >= 5000, "Постоянный",
    IF([Сумма] >= 1000, "Новый", "Потенциальный")
  )
)`}
        </pre>

        <p><b>SWITCH:</b></p>

        <pre>
{`SWITCH([Статус],
"Новый", "🟢",
"В обработке", "🟡",
"Доставлен", "🔵",
"Отменен", "🔴",
"⚪")`}
        </pre>

        <p>Логические функции:</p>

        <pre>
{`AND([Статус] = "Новый", [Сумма] > 1000)
OR([Приоритет] = "Высокий", [Дедлайн] < TODAY())
NOT([Статус] = "Завершен")`}
        </pre>

        <p>Пример сложного условия:</p>

        <pre>
{`AND(
[Статус] <> "Завершен",
OR([Приоритет] = "Высокий", [Дедлайн] < TODAY()),
ISNOTBLANK([Ответственный])
)`}
        </pre>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Как обратиться к связанным данным?</li>
          <li>Что такое виртуальный столбец?</li>
          <li>Когда использовать SWITCH?</li>
          <li>Напишите формулу с AND</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 7 ================= */

function Lecture7() {
  return (
    <>
      <h1>📘 Лекция 7. Действия и боты</h1>

      <div className="card">
        <h2>1. Что такое действия (Actions)</h2>

        <p>
          Действия — это механизм AppSheet, позволяющий выполнять операции 
          при взаимодействии пользователя с интерфейсом или автоматически.
        </p>

        <p>Основные типы действий:</p>

        <p><b>Навигация:</b></p>
        <ul>
          <li>Переход к виду</li>
          <li>Переход по ссылке</li>
        </ul>

        <p><b>Работа с данными:</b></p>
        <ul>
          <li>Добавить запись</li>
          <li>Обновить запись</li>
          <li>Удалить запись</li>
        </ul>

        <p><b>Взаимодействие:</b></p>
        <ul>
          <li>Отправить email</li>
          <li>Показать диалог</li>
        </ul>

        <p>
          Каждое действие имеет параметры: Name, Target, Type, Data, Condition.
        </p>
      </div>

      <div className="card">
        <h2>2. Создание пользовательских действий и кнопок</h2>

        <p>
          Пользовательские действия позволяют добавить кнопки в интерфейс приложения.
        </p>

        <p>Где можно разместить кнопки:</p>

        <ul>
          <li>В видах (Views)</li>
          <li>В формах</li>
          <li>В детальной карточке</li>
          <li>В таблице как столбец</li>
          <li>В меню навигации</li>
        </ul>

        <p>Как создать действие:</p>

        <ol>
          <li>Automation → Actions</li>
          <li>Create New Action</li>
          <li>Выбрать тип</li>
          <li>Настроить параметры</li>
          <li>Добавить Condition</li>
          <li>Сохранить</li>
        </ol>

        <p>Примеры условий:</p>

        <ul>
          <li>[Статус] &lt;&gt; "Выполнено"</li>
          <li>[Создал] = USEREMAIL()</li>
          <li>ISNOTBLANK([Клиент])</li>
        </ul>
      </div>

      <div className="card">
        <h2>3. Что такое боты (Bots)</h2>

        <p>
          Боты — это автоматические процессы, которые запускаются при наступлении событий.
        </p>

        <p>Бот состоит из:</p>

        <ol>
          <li>Trigger (событие)</li>
          <li>Process (действия)</li>
          <li>Condition (условие)</li>
        </ol>

        <p>Настройка Data Change:</p>

        <ul>
          <li>Table — таблица</li>
          <li>Event Type — Create / Update / Delete</li>
          <li>Condition — формула</li>
        </ul>

        <p>Пример:</p>

        <pre>
{`[Статус] = "Новый" AND ISNOTBLANK([Клиент].[Email])`}
        </pre>

        <p>Scheduled (по расписанию):</p>

        <ul>
          <li>Schedule — период</li>
          <li>Time — время</li>
          <li>Data — срез</li>
        </ul>

        <p>Пример:</p>

        <pre>
{`Schedule = Daily
Time = 09:00
Data = "Просроченные_заказы"`}
        </pre>
      </div>

      <div className="card">
        <h2>4. Отправка уведомлений</h2>

        <p><b>Email:</b></p>

        <p>
          Параметры: To, Subject, Body.
        </p>

        <p>Пример:</p>

        <pre>
{`CONCATENATE(
"Здравствуйте, ", [Клиент].[Имя], "\\n\\n",
"Ваш заказ №", [Номер заказа], " от ", TEXT([Дата], "dd.MM.yyyy"),
" подтвержден.\\n\\n",
"Сумма заказа: ", TEXT([Сумма], "#,##0"), " руб.\\n",
"Статус: ", [Статус], "\\n\\n",
"Спасибо за покупку!"
)`}
        </pre>

        <p><b>Push-уведомления:</b></p>

        <p>
          Настройка включает Recipients, Title, Body, Link.
        </p>

        <p>Пример:</p>

        <pre>
{`CONCATENATE(
"Поступил заказ №", [Номер заказа],
" от ", [Клиент].[Название]
)`}
        </pre>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Какие типы действий существуют?</li>
          <li>Где размещаются кнопки?</li>
          <li>Что такое бот?</li>
          <li>Разница Data Change и Scheduled?</li>
          <li>Как сделать push-уведомление?</li>
        </ol>
      </div>
    </>
  );
}

/* ================= ЛЕКЦИЯ 8 ================= */
function Lecture8() {
  return (
    <>
      <h1>📘 Лекция 8. Доступ, публикация и установка приложения</h1>

      <div className="card">
        <h2>1. Идентификация пользователей и основные функции</h2>

        <p>
          AppSheet идентифицирует пользователей по их email-адресу. Все правила доступа строятся вокруг этого.
        </p>

        <p>Функции для работы с пользователем:</p>

        <ul>
          <li>USEREMAIL() — возвращает email текущего пользователя</li>
          <li>USERNAME() — возвращает имя текущего пользователя</li>
        </ul>

        <p>
          Эти функции можно использовать в фильтрах, виртуальных столбцах и условиях для действий.
        </p>
      </div>

      <div className="card">
        <h2>2. Уровни прав доступа</h2>

        <p>Права настраиваются на четырёх уровнях:</p>

        <p><b>Таблица (Data → Tables → Security)</b></p>

        <p>Четыре типа прав: Create, Read, Update, Delete.</p>

        <p>Примеры:</p>

        <ul>
          <li>Create только администраторам</li>
          <li>Update только автору записи</li>
          <li>Delete никому</li>
        </ul>

        <pre>
{`IN(USEREMAIL(), "admin@company.com", "manager@company.com")

USEREMAIL() = [Создал]

FALSE`}
        </pre>

        <p><b>Срез (Slice)</b> — виртуальная копия таблицы с фильтром.</p>

        <p>
          Пример: срез «Мои задачи» с фильтром [Ответственный] = USEREMAIL()
        </p>

        <p><b>Вид (View)</b> — управляет доступом к экранам.</p>

        <p>Пример:</p>

        <pre>
{`IN(USEREMAIL(), "admin@company.com")`}
        </pre>

        <p><b>Столбец (Column Security)</b>:</p>

        <ul>
          <li>Show if — отображение</li>
          <li>Editable if — редактирование</li>
          <li>Required if — обязательность</li>
        </ul>
      </div>

      <div className="card">
        <h2>3. Row Level Security (RLS)</h2>

        <p>
          RLS — механизм, показывающий пользователям только те записи, к которым у них есть доступ.
        </p>

        <p><b>Типовые сценарии:</b></p>

        <p>Пользователь видит только свои записи:</p>

        <pre>
{`[Ответственный] = USEREMAIL()`}
        </pre>

        <p>Администратор видит всё, остальные — только свои:</p>

        <pre>
{`IF(
   IN(USEREMAIL(), "admin@company.com"),
   TRUE,
   [Ответственный] = USEREMAIL()
)`}
        </pre>

        <p>
          Практический подход: создайте отдельные срезы для разных групп пользователей 
          («Мои задачи», «Задачи отдела», «Все задачи»).
        </p>

        <p>
          Автозаполнение: в Initial Value можно указать USEREMAIL().
        </p>
      </div>

      <div className="card">
        <h2>4. Публикация приложения</h2>

        <p>Режимы доступа:</p>

        <ul>
          <li>Private — только автор</li>
          <li>Domain — пользователи домена</li>
          <li>White list — список email</li>
          <li>Public — любой пользователь</li>
        </ul>

        <p><b>Приглашение пользователей:</b></p>

        <ol>
          <li>Add Users</li>
          <li>Ввести email</li>
          <li>Выбрать уровень доступа</li>
        </ol>
      </div>

      <div className="card">
        <h2>5. Установка на устройства и распространение</h2>

        <p><b>Способы установки:</b></p>

        <ul>
          <li>AppSheet Mobile App</li>
          <li>Веб-приложение</li>
          <li>PWA (на главный экран)</li>
        </ul>

        <p><b>Как распространить:</b></p>

        <ul>
          <li>Ссылка</li>
          <li>QR-код</li>
          <li>Email-приглашение</li>
        </ul>

        <p>
          Офлайн-режим: приложение работает без интернета, а потом синхронизируется.
        </p>
      </div>

      <div className="card">
        <h2>❓ Контрольные вопросы</h2>

        <ol>
          <li>Какие функции возвращают email и имя пользователя?</li>
          <li>На каких уровнях настраиваются права доступа?</li>
          <li>Как написать фильтр для своих записей?</li>
          <li>Разница White list и Domain?</li>
          <li>Как установить приложение на экран телефона?</li>
          <li>Почему изменения не видны сразу?</li>
        </ol>
      </div>
    </>
  );
}
